# Crop-Disease-Detection-DeepLearning
A deep learning–based plant disease detection system that classifies plant leaf images into healthy and diseased categories using convolutional neural networks (CNN).
